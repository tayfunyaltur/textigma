import React from "react";

const TextInput = () => {
    return (
        <input
            type="text"
            className=" px-2 bg-transparent border-2 border-black focus-visible:outline-0 focus-visible:border-black focus-within:ring-2 focus-within:ring-blue focus:bg-blue"
        />
    );
};

export default TextInput;
